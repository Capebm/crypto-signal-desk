# Optional: point the UI to your own server-side AI explanation endpoint.
# Never expose an AI provider secret in a Vite client variable.
# VITE_AI_EXPLANATION_URL=https://your-server.example.com/api/explain-signal
