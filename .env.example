# Optional: Anthropic API key for worldwide web hunt + PT price estimates
# NEVER expose this in a Vite client variable — only server-side / Netlify functions
ANTHROPIC_API_KEY=sk-ant-...

# Optional: legacy AI explanation endpoint for crypto desk
# VITE_AI_EXPLANATION_URL=https://your-server.example.com/api/explain-signal
